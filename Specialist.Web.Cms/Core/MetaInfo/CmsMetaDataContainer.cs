
    
//            this.Display("Город");
//                this.DisplayBy(x => x.CityName);
//                For(x => x.City_TC).Display( "TC");
//                For(x => x.Email).Display( "Email");
//                For(x => x.Phones).Display( "Телефоны");
//                For(x => x.CityName).Display( "Город");
//                For(urlName);
//                For(description);
//                ;
//            this.Display("Комплекс");
//              this.DisplayByName();
//              this.AddName();
//              For(urlName);
//              For(x => x.IsPublished).Display( "Использовать на сайте");
//              For(x => x.WorkingHours).Display( "Время работы");
//              For(x => xForress).Display( "Адрес");
//              For(description);
//              .IsReadOnly();
//              ;

//            this.Display("Тип группировки");
//                this.DisplayByName();
//                this.AddName();
//                For(urlName);
//                ;
//            this.Display("Группировка");
//                this.DisplayByName();
//                For(x => x.CustomSectionType_ID).Display( "Тип группировки").Display(
//                     ControlNames.ComboBox);
//                this.AddName();
//                For(urlName);
//                ;


  
         /*   this.Display("Страница");
                this.Deletable();
                this.DisplayBy(x => x.UrlName);
                ForActionLink("Контент").Display( "EditContent");
                For(urlName);
                For(x => x.ParentID).Display( "Родитель").Display( ControlNames.ComboBox);
                For(x => x.Name).Display( "Заголовок");
                For(x => x.Template).Display( "Шаблон").Display( typeof (Template));
                For(x => x.Keywords).Display( "Ключевые слова");
                For(x => x.JavaScriptFiles).Display( "Пути к JavaScript файлам(через запятую)");
                ;
            this.Display("Блок");
                For(isActive);
                For(x => x.TypedSetting).Display( "");
                ;
            this.Display();
                For(description);
                ;
            this.Display();
                .IsReadOnly();
                For(x => x.BannerID).Display( "Баннер").Display( typeof (Banner));
                ;
            this.Display();
                For(x => x.MaxLength).Display( "Максимальная длинна");
                For(x => x.ShowImages).Display( "Отображать картинки");
                ;
            this.Display();
                For(x => x.MaxCourseCount).Display( "Максимальная количество курсов");
                ;
*/

         /*   this.Display();
               this.DisplayBy(x => x.Title);
               For(x => x.Title).Display("Заголовок");
               For(x => x.Text).Display("Содержание").Display( ControlNames.Html);
               For(x => x.PublishDate).Display( "Дата публикации");
               For(isActive);
               ;*/
