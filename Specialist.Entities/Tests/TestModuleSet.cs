using System.Collections.Generic;
using SimpleUtils.Utils;
using System.Linq;
using SimpleUtils.Common.Extensions;

namespace Specialist.Entities.Tests {
	public partial class TestModuleSet {

/*
        public List<int> ModuleIds {
            get { return StringUtils.SafeSplit(ModuleList)
				.Select(int.Parse).ToList(); }
			set {
				if(value == null) {
					ModuleList = string.Empty;
				} else {
					ModuleList = value.Select(x => x.ToString()).JoinWith(",");
				}
			}
        }
*/
		 
	}
}